const path = require('path'); //para lidar com caminhos do node

module.exports = {
    //Caminho para o arquivo de config do banco (credencias e etc)
    config: path.resolve(__dirname, 'src', 'config', 'database.js'), 
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'),
}